version: 2
jobs:
      dawn:
            docker:
                  - image: ubuntu:eoan
            steps:
                  - checkout:
                          path: /usr/src/dawn
                  - run:
                          name: Something
                          command: cat /etc/*release
